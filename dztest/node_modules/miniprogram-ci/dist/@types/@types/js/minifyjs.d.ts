import type { RawSourceMap } from 'source-map';
interface IOptions {
    code: string;
    useTerser: boolean;
    filePath: string;
    inputSourceMap?: RawSourceMap;
}
declare const minify: (options: IOptions) => import("terser").MinifyOutput | import("uglify-js").MinifyOutput | {
    error: {
        message: string;
        code: number;
    };
};
export = minify;
